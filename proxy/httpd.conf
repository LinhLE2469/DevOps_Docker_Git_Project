ServerRoot "/usr/local/apache2"
Listen 80

LoadModule mpm_event_module modules/mod_mpm_event.so
LoadModule authn_core_module modules/mod_authn_core.so
LoadModule authz_core_module modules/mod_authz_core.so
LoadModule access_compat_module modules/mod_access_compat.so
LoadModule unixd_module modules/mod_unixd.so
LoadModule dir_module modules/mod_dir.so
LoadModule alias_module modules/mod_alias.so
LoadModule log_config_module modules/mod_log_config.so
LoadModule mime_module modules/mod_mime.so
LoadModule negotiation_module modules/mod_negotiation.so
LoadModule headers_module modules/mod_headers.so

# Proxy modules
LoadModule proxy_module modules/mod_proxy.so
LoadModule proxy_http_module modules/mod_proxy_http.so

ServerName localhost
ServerAdmin you@example.com

DocumentRoot "/usr/local/apache2/htdocs"
<Directory "/usr/local/apache2/htdocs">
    Options Indexes FollowSymLinks
    AllowOverride None
    Require all granted
    DirectoryIndex index.html
</Directory>

# Basic logging to stdout/stderr (useful when running in a container)
ErrorLog /proc/self/fd/2
CustomLog /proc/self/fd/1 common

# Disable forward proxy
ProxyRequests Off

# Allow proxying to backend
<Proxy *>
    Require all granted
</Proxy>

# Reverse proxy VirtualHost: forward traffic to the backend service
# Exclude specific static paths from proxying so DocumentRoot files are served
<VirtualHost *:80>
    ProxyPreserveHost On
    # Don't proxy the landing page and common static locations
    ProxyPass "/index.html" "!"
    ProxyPass "/static/" "!"
    ProxyPass "/favicon.ico" "!"

    # Proxy everything else to backend
    ProxyPass "/" "http://backend:8080/"
    ProxyPassReverse "/" "http://backend:8080/"

    # Serve static files from DocumentRoot before proxying
</VirtualHost>

# End of file